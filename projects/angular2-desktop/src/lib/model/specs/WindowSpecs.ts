import {ShortCutSpecs} from './ShortCutSpecs';

export class WindowSpecs {
  state:number;
  active:boolean;
  order:number;
  title:string;
  x: number;
  y: number;
  width: number;
  height: number;
  shortCut:ShortCutSpecs;
}
